[{{#if item.authors~}}{{#each item.authors as |author|~}}
    {{~#each author as |name|~}}
        {{~name~}}
        {{~#unless @last}} {{/unless~}}
    {{~/each}}
{{~#unless @last}}, {{/unless~}}
{{~/each}}. {{/if~}}
{{~#if item.title}}<cite>{{item.title}}</cite>. {{/if}}
{{~#if item.pub_year}}{{item.pub_year}}. {{/if}}]({{path}}#{{item.citation_key}})